import * as React from 'react'
import * as ReactDOM from 'react-dom'
import BankName from './components/api'
import App from './components/app'

ReactDOM.render(
  <App/>,
  document.getElementById('app'),
)

{/* ReactDOM.render(
  <div>
    <App />
  document.getElementById('app'),
) */}
